# Local MAESTRO run configuration.  It intentionally contains only ORCA so
# validation does not depend on unrelated, unavailable engine installations.

[env.orca]
kind = "module"
modules = ["orca/v6.0.1"]

[exec.orca]
exec_dir = "/appl/share/orca_6_0_1_linux_x86-64_shared_openmpi416"
exec_cmd = "orca"
